Backup-only implementations for ticker/quote resolver experiments.

These files are intentionally isolated from the main application flow.
Do not import them from production paths unless the resolver approach is reintroduced.
